# 宠物兔成长记录小程序需求整理

## 1. 项目目标

开发一个用于记录宠物兔子成长情况的轻量应用，优先考虑在 iOS 上简单可运行，技术形态倾向于 `HTML/CSS` 网页或接近小程序体验的移动端 Web 页面。

核心目标：

- 记录兔子的每日体重与备注
- 根据体重自动计算当日兔粮需求
- 通过图表和表格查看成长趋势
- 监控天气温度并给出养兔提醒
- 提供按周期刷新的日常照护 checklist

## 2. 产品定位与使用场景

- 使用对象：宠物兔主人
- 使用终端：手机端，重点兼容 iOS
- 使用方式：每日记录体重、查看备注、根据天气调整照护、完成日常检查事项
- 所有用户输入和业务数据都需要长期保存，不能只保存在当前页面会话中

## 3. 页面结构

当前需求明确仅包含 2 个主页面：

1. 每日记录
2. 每日监控

根据参考图，界面整体是移动端单页卡片式布局，偏 iPhone Web App 风格。

## 4. 页面一：每日记录

### 4.1 模块结构

页面包含 3 个主要区域：

1. 体重变化趋势
2. 日历
3. 备注详情

### 4.2 体重变化趋势模块

需要实现：

- 一张体重走势图
- 图表支持切换显示区间：`周` / `月`
- 使用 `1` 个切换按钮在 `周` / `月` 之间切换，优先简单直观
- 图表下方展示每日体重表格
- 提供 `新增记录` 按钮

交互要求：

- 点击 `新增记录` 按钮后，弹出详情记录页
- 在主界面点击 `新增记录` 时，若当天尚无记录，则追加当天记录
- 在主界面点击 `新增记录` 时，若当天已有记录，则跳转到当天记录的编辑界面
- 当天记录编辑界面可修改体重、备注、图片等内容

表格字段如下：

- 日期
- 体重
- 增量
- 兔粮量
- 备注

字段规则：

- `增量`：与前一天体重对比的差值
- `兔粮量`：根据体重自动计算当日需求
- `备注`：表格中仅显示备注标题，不展开全文
- 第一天记录时，`增量` 显示为 `0`
- 同一天不能存在多条记录，但允许再次打开并更新当天内容
- 体重单位固定为 `g`
- 兔粮量保留整数即可

表格交互：

- 表格区域可上下滑动

### 4.3 兔粮计算规则

按时间阶段使用不同百分比计算：

- `2026年4月以内`：体重的 `7%`
- `2026年5月以内`：体重的 `6%`
- `2026年6月以内`：体重的 `5%`
- `2026年7月以内`：体重的 `4%`
- `2026年8月以内`：体重的 `3%`
- `2026年9月开始`：保持 `2%`

已确认：兔粮比例按真实日期推进，根据记录当天日期命中对应月份区间。

### 4.4 日历模块

需要实现：

- 显示月历
- 可选择某一天
- 选中某日期后，在旁边或下方联动显示该日期对应的备注详情

### 4.5 备注详情模块

展示内容包括：

- 备注标题
- 今日体重
- 详细记录

详细记录支持：

- 文本内容
- 添加图片

数据要求：

- 备注图片需要持久保存
- 每天记录仅保存 `1` 张图片
- 图片支持替换
- 图片来源支持从相册选择

### 4.6 详情记录页

详情记录页内容基本等同于备注详情模块，用于新增或编辑当天记录。

页面字段包括：

- 标题
- 体重输入
- 详情记录
- 添加 `1` 张图片

交互规则：

- 从主界面点击 `新增记录` 进入
- 如果今天没有记录，则进入新增状态并创建今天的记录
- 如果今天已有记录，则进入今天记录的编辑状态
- 编辑状态下可更新标题、体重、详情记录和图片

### 4.7 每日记录页建议数据结构

- 日期
- 体重
- 增量
- 兔粮量
- 备注标题
- 备注正文
- 备注图片列表

持久化要求：

- 每日记录相关数据需要长期保存
- 包括日期、体重、增量、兔粮量、标题、备注正文、图片

## 5. 页面二：每日监控

页面分为 2 大块：

1. 温度监控
2. Checklist

### 5.1 温度监控模块

#### 地区选择

- 顶部可选择当前都道府県
- 默认地区：`东京都日野市`
- 天气数据需要接入真实天气 API
- 允许切换其他日本城市
- 可切换的地区范围根据所选气象 API 的支持范围决定
- 用户当前选择的地区需要长期保存

#### 天气展示

下方分左右两个卡片：

- 左侧：今日气温与天气
- 右侧：明日气温与天气

建议展示信息：

- 日期
- 天气图标/天气类型
- 当前温度
- 最高温 / 最低温

#### 温度警示规则

- 当最高温高于 `27°C` 时，显示 `高温注意` 警示框
- 当最低温低于 `15°C` 时，显示 `低温注意` 警示框
- 常温时显示普通状态，无强警示

异常与刷新规则：

- 天气接口请求失败时，显示失败信息
- 地区切换后如果天气获取失败，同样显示失败信息
- 每次打开网页时实时请求并显示天气

#### 兔子状态图标

根据温度状态切换对应兔子插图：

- 高温状态：炎热提示图
- 低温状态：保暖提示图
- 常温状态：普通/舒适状态图

图片素材映射规则：

- `图片素材.png` 为 `2 x 3` 排布，共 `6` 张兔子图片
- 第 `1` 列第 `1` 行图片：用于高温预警状态
- 第 `2` 列第 `1` 行图片：用于低温预警状态
- 第 `1` 列第 `2` 行图片：用于常温状态
- 剩余图片当前版本不使用，保留给后续新增功能

## 6. Checklist 模块

### 6.1 功能要求

- 展示若干照护 checklist 条目
- 每个条目可打勾，表示已完成
- 条目具备周期属性
- checklist 完成状态需要持久保存
- 勾选框允许取消勾选
- checklist 条目配置与当前完成状态都需要长期保存
- 已完成的 checklist 行背景改为淡绿色
- 当用户勾选完成时，在勾选框周围显示一只小兔子弹出的提示动效
- 该小兔子动效使用项目内素材 `ListClear.png`
- `ListClear.png` 中的白色背景在前端展示时需要做去白处理，使素材尽量以透明效果融入界面
- `待办事项` 区域增加一个 `编辑` 按钮
- `编辑` 按钮放在标题 `待办事项` 右侧，右上角区域单独留给状态插图展示
- 通过编辑入口可以新增、修改、删除 checklist 条目
- 每个条目可编辑的内容包括：list 内容、是否重复
- 如果条目为可重复，则可从 `每日`、`每周`、`每两周`、`每两月` 中选择
- 如果条目为不可重复，则通过日期选择开始日期与结束日期
- 不可重复条目仅在开始日期到结束日期范围内显示
- `待办事项` 卡片右上角增加基于 `图片素材.png` 的状态兔子插图
- 当今天必须完成的 Checklist 全部完成时，显示 `1-3`
- 当今天必须完成的 Checklist 仍有未完成项时，显示 `2-3`
- 当今天必须完成的 Checklist 仍有未完成项，且日本时区时间已超过 `22:00` 时，显示 `2-2`
- 增加一条隐藏彩蛋待办：`夸夸你的亲亲男朋友`
- 该彩蛋待办不会出现在编辑栏内，用户不能通过待办编辑功能新增、修改或删除它
- 该彩蛋待办会参与当天待办列表展示、勾选完成、完成统计和状态兔子判定
- `2026-04-26` 与 `2026-04-30` 这两天该彩蛋待办固定出现
- 从 `2026-05` 开始，每个月会随机抽取 2 天，在对应日期显示该彩蛋待办

支持的周期类型：

- 每日
- 每周
- 每两周
- 每两月

对于 `每周 / 每两周 / 每两月` 类型：

- 需要明确显示该 checklist 对应的时间区间

### 6.2 刷新规则

- checklist 刷新仅在用户刷新网页时触发判定
- 每天早上 `4:00` 后，刷新网页时重置每日 checklist
- 周单位 checklist 在每周一刷新网页时更新
- 每两周 checklist 以周一为基准日，在满足两周周期时于刷新网页时更新
- 每两月 checklist 以双月周期刷新
- checklist 不需要保留历史记录
- checklist 不支持补打卡

### 6.3 默认 checklist

- 每日 喂食
- 每日 喂零食
- 每日 加水
- 每日 铲屎
- 每日 称重
- 每两周 剪指甲

编辑能力补充：

- 默认 checklist 仅作为初始化内容，后续可被用户修改
- 用户可新增自定义 checklist 条目
- 用户可修改默认条目和自定义条目的文案与周期
- 用户可删除已有条目

## 7. 视觉与风格要求

根据参考图，界面风格可整理为：

- 主色调：浅蓝、奶油白、淡粉橙点缀
- 氛围：柔和、治愈、可爱、轻拟物插画风
- 布局：大圆角卡片、分区明显、留白充足
- 终端感受：接近 iOS App / 小程序首页视觉
- 插画素材：使用兔子插图表达状态，高温/低温/进食/记录等场景
- 背景：可使用浅蓝渐变或插画背景

标题区补充要求：

- 顶部主标题文案改为 `モンブラン日記`
- 主标题下方副标题文案改为 `Presented by your dearest Lemon`
- 副标题末尾增加一个柠檬小图标
- 主标题胶囊区域周围增加装饰性图形元素，如爱心、叶片等
- 主标题胶囊的边框颜色改为浅棕色
- 主标题胶囊内部底色也改为浅棕奶油色，而不是纯白

## 8. 从参考图补充出的界面特征

可作为实现方向参考：

- 顶部使用大标题卡片
- 模块以白色圆角卡片承载
- 关键操作使用蓝色高亮按钮
- 图表、日历、详情卡片并列或上下堆叠展示
- 底部可采用移动端导航栏样式
- 页面需显式包含 `新增记录`、`周/月切换`、`地区选择`、`checklist勾选` 等关键交互按钮/入口
- `周/月切换` 采用单按钮切换形式，不需要复杂分段控件

文案与展示补充：

- 原“备注详情”模块标题改为 `摘要`
- 新增/编辑记录弹层中的“标题”字段也改为 `摘要`
- `摘要` 字段允许留空，不强制填写
- 监控页删除以下辅助文案：
- `实时天气与地区数据来自日本气象庁`
- `当前地区：...`
- `按 都道府県 → 市町村 的顺序选择地区。`
- 所有按钮增加点击后轻微回弹的可爱微动画

## 9. 当前可确定的功能清单

- 记录每日体重
- 生成体重趋势图
- 周/月维度切换
- 自动计算兔粮量
- 展示体重表格
- 通过日历查看某日备注
- 为备注添加图片
- 基于地区查看今日/明日天气
- 根据温度触发高温/低温提醒
- 根据温度展示不同兔子状态图
- 展示并勾选周期性 checklist
- 按日/周/月规则自动刷新 checklist
- 图表仅展示体重数据
- 图表在周/月视图下以最新记录日期为锚点显示数据；若最新记录日期晚于今天，则优先按最新记录日期取范围

## 10. 待确认事项

当前无

## 11. 推荐的最小可交付范围

如果先做第一版，建议最小范围如下：

- 2 个页面：每日记录、每日监控
- 持久化保存所有业务数据，包括体重记录、备注图片、checklist 状态、地区选择等
- 每日记录页完成图表、表格、日历、详情联动
- 每日监控页接入真实天气 API，并完成天气展示、温度告警、兔子状态图切换
- 支持默认 checklist 与自动刷新逻辑
- 完成移动端适配，确保 iPhone 浏览器可正常使用
- 第一版不要求离线可用
- 当前交付版本采用 GitHub Pages 友好的分文件结构：`index.html`、`styles.css`、`app.js` 与图片素材分别独立存放并相互引用

## 12. 数据持久化范围

以下内容都需要长期保存：

- 每日记录日期
- 体重
- 增量
- 兔粮量
- 标题
- 详情记录正文
- 当天图片
- checklist 条目内容
- checklist 勾选状态
- 当前选择的城市或地区

说明：

- 天气接口返回结果本身不要求长期保存，只需在页面打开时实时获取并展示
- 需要长期保存的是用户产生、维护或选择的业务数据

## 13. 空状态与异常状态

- 没有任何每日记录时，页面显示 `尚无记录`
- 某一天没有备注或图片时，详情区域显示空状态提示
- 天气数据加载失败时，显示明确失败信息

## 14. 已归档的近期界面调整

- 顶部品牌标题已从中文改为日文风格标题 `モンブラン日記`
- 副标题改为署名式文案 `Presented by your dearest Lemon`
- 标题区域新增爱心与叶片装饰元素
- 标题外框与内部底色统一调整为浅棕奶油色
- 项目已从单文件内嵌版本恢复为常规分文件结构，便于在 GitHub Pages 上继续维护
- 背景图与 checklist 完成动效图已恢复为外部图片文件引用，不再以内联 base64 形式直接塞入主页面
- 顶部重复的页面切换按钮已删除，仅保留底部固定导航做 `每日记录 / 每日监控` 切换
- `モンブラン日記` 与 `Presented by your dearest Lemon` 所在标题区改为滚动时保持固定的吸顶区域
- 页面背景改为滚动时保持固定，并针对 iPhone / iOS 屏幕缩小背景兔子图尺寸，避免显示过大和裁切过多
- “备注详情”统一改称“摘要”
- 摘要字段改为可选输入，不再强制必填
- 监控页删去来源说明、当前地区展示和选择提示语，只保留必要控件
- 图表范围计算已兼容调试和未来日期记录场景，正式逻辑保留为“锚定最新记录日期”

## 15. 结论

这是一个以“宠物兔成长记录 + 日常照护提醒”为核心的移动端轻应用需求，重点不在复杂业务，而在：

- 记录体验简单
- 信息展示清晰
- 视觉风格可爱治愈
- 自动化规则足够贴合日常养兔场景

从需求完整度来看，已经足够进入低保真原型或第一版前端实现阶段。
